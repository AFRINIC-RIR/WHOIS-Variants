<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
                           http://camel.apache.org/schema/spring
                           http://camel.apache.org/schema/spring/camel-spring.xsd">

    <import resource="classpath*:/whois-core-context.xml"/>

    <camelContext xmlns="http://camel.apache.org/schema/spring">

        <!-- Exception handler -->
        <onException>
            <exception>whois.core.api.CommandException</exception>
            <handled>
                <constant>true</constant>
            </handled>
            <bean ref="ExceptionService" method="commandFailed"/>
        </onException>

        <!-- Load balancer -->
        <route id="restlet-query-convert">
            <from uri="restlet:http://localhost:4480/{id}/{format}?restletMethod=get"/>
            <loadBalance>
                <roundRobin/>
                <to uri="direct:restlet-query-convert-4481"/>
                <to uri="direct:restlet-query-convert-4482"/>
                <to uri="direct:restlet-query-convert-4483"/>
            </loadBalance>
        </route>

        <route id="restlet-query-convert-4481">
            <from uri="direct:restlet-query-convert-4481"/>
            <transform>
                <simple>${header.id}</simple>
            </transform>
            <setHeader headerName="command">
                <constant>query</constant>
            </setHeader>
            <bean ref="WhoisService" method="messageReceived"/>
            <setHeader headerName="conversion">
                <simple>rpsl2${header.format}</simple>
            </setHeader>
            <bean ref="TextConvertService" method="messageReceived"/>
            <to uri="log:whois.core.api.QueryCommand?level=INFO&amp;multiline=true&amp;skipBodyLineSeparator=false"/>
            <to uri="velocity:whois-query.vm"/>
        </route>

        <route id="restlet-query-convert-4482">
            <from uri="direct:restlet-query-convert-4482"/>
            <transform>
                <simple>${header.id}</simple>
            </transform>
            <setHeader headerName="command">
                <constant>query</constant>
            </setHeader>
            <bean ref="WhoisService" method="messageReceived"/>
            <setHeader headerName="conversion">
                <simple>rpsl2${header.format}</simple>
            </setHeader>
            <bean ref="TextConvertService" method="messageReceived"/>
            <to uri="log:whois.core.api.QueryCommand?level=INFO&amp;multiline=true&amp;skipBodyLineSeparator=false"/>
            <to uri="velocity:whois-query.vm"/>
        </route>

        <route id="restlet-query-convert-4483">
            <from uri="direct:restlet-query-convert-4483"/>
            <transform>
                <simple>${header.id}</simple>
            </transform>
            <setHeader headerName="command">
                <constant>query</constant>
            </setHeader>
            <bean ref="WhoisService" method="messageReceived"/>
            <setHeader headerName="conversion">
                <simple>rpsl2${header.format}</simple>
            </setHeader>
            <bean ref="TextConvertService" method="messageReceived"/>
            <to uri="log:whois.core.api.QueryCommand?level=INFO&amp;multiline=true&amp;skipBodyLineSeparator=false"/>
            <to uri="velocity:whois-query.vm"/>
        </route>

    </camelContext>

</beans>